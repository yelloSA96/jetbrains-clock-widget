# jb-clock-widget

A JetBrains Platform plugin: a status bar clock widget. See `.docs/design.md` for the full design (scope, format, placement, non-goals).

For guided, step-by-step teaching while building this, use the `tutorial-mode` skill (`/tutorial-mode`).
