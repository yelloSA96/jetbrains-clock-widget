# Cheatsheet

## Development 
Testing the plugin inside a sanboxed IDE with the plugin.
```bash
./gradlew runIde
```

### Packaging Plugin
```bash
./gradlew buildPlugin
```
Look inside `build/distributions/*.zip`